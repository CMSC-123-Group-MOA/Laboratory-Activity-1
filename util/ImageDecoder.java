package util;

import java.awt.image.BufferedImage;
import java.io.File;

import ui.Controller;

public class ImageDecoder {
    public static BufferedImage decode(Controller controller, File hufPath, File cmpPath) {
        BufferedImage decodedImage = new BufferedImage(0, 0, 0);
        
        return decodedImage;
    }
}
